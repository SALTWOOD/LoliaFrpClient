<?xml version="1.0" encoding="utf-8"?>

<Page
    x:Class="LoliaFrpClient.Pages.FrpcManagerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LoliaFrpClient.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 左侧：进程列表 -->
        <Border Grid.Column="0"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Padding="16"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                Margin="0,0,12,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="运行中的实例"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,12" />

                <ListView Grid.Row="1"
                          x:Name="ProcessListView"
                          ItemsSource="{x:Bind RunningProcesses, Mode=OneWay}"
                          SelectionChanged="OnProcessSelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:FrpcProcessDisplayItem">
                            <Grid Padding="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0"
                                        Width="10"
                                        Height="10"
                                        CornerRadius="5"
                                        Background="{x:Bind StatusColor, Mode=OneWay}"
                                        Margin="0,0,8,0"
                                        VerticalAlignment="Center" />

                                <StackPanel Grid.Column="1" Spacing="2">
                                    <TextBlock Text="{x:Bind DisplayName, Mode=OneWay}"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{x:Bind StatusText, Mode=OneWay}"
                                               FontSize="12"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- 空状态提示 -->
                <TextBlock Grid.Row="1"
                           x:Name="EmptyStateText"
                           Text="没有运行中的实例"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Visibility="Collapsed" />
            </Grid>
        </Border>

        <!-- 右侧：详情和日志 -->
        <Border Grid.Column="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Padding="16"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- 标题和控制按钮 -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               x:Name="DetailTitle"
                               Text="选择一个实例查看详情"
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center" />

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                Spacing="8"
                                x:Name="ControlButtons"
                                Visibility="Collapsed">
                        <Button Content="重启"
                                Click="OnRestartClick"
                                Style="{StaticResource AccentButtonStyle}" />
                        <Button Content="停止"
                                Click="OnStopClick" />
                        <Button Content="清空日志"
                                Click="OnClearLogClick" />
                    </StackPanel>
                </Grid>

                <!-- 实例信息 -->
                <Grid Grid.Row="1"
                      x:Name="ProcessInfoPanel"
                      Visibility="Collapsed"
                      Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock Text="进程 ID"
                                   FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="ProcessIdText"
                                   FontWeight="SemiBold" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Spacing="4">
                        <TextBlock Text="启动时间"
                                   FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="StartTimeText"
                                   FontWeight="SemiBold" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Spacing="4">
                        <TextBlock Text="运行时长"
                                   FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="RunningTimeText"
                                   FontWeight="SemiBold" />
                    </StackPanel>
                </Grid>

                <!-- 日志输出 -->
                <Border Grid.Row="2"
                        Background="{ThemeResource LayerFillColorDefaultBrush}"
                        CornerRadius="4"
                        Padding="8">
                    <ScrollViewer x:Name="LogScrollViewer"
                                  VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="LogOutputText"
                                   FontFamily="Consolas"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   Text="选择一个实例查看日志输出" />
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Page>