<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LoliaFrpClient.Pages.Page4"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LoliaFrpClient.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:services="using:LoliaFrpClient.Services"
    mc:Ignorable="d">

    <Grid Padding="20">
        <StackPanel Spacing="16">
            <!-- 标题和刷新按钮 -->
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                           Text="节点列表" 
                           Style="{StaticResource TitleTextBlockStyle}"/>
                
                <Button Grid.Column="1" 
                        Content="刷新" 
                        Click="OnRefreshClick"/>
            </Grid>

            <!-- 节点列表 -->
            <ListView x:Name="NodesListView"
                      ItemsSource="{x:Bind Nodes, Mode=OneWay}"
                      SelectionMode="Single"
                      IsItemClickEnabled="True"
                      ItemClick="OnNodeItemClick">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="services:NodeInfo">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                CornerRadius="8"
                                Padding="16"
                                Margin="0,0,0,8"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1">
                            <Grid ColumnSpacing="12" RowSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0"
                                           Text="{x:Bind Name, Mode=OneWay}"
                                           Style="{StaticResource SubtitleTextBlockStyle}"
                                           FontWeight="SemiBold"/>

                                <Border Grid.Row="0" Grid.Column="1"
                                        Background="{x:Bind StatusBrush, Mode=OneWay}"
                                        Padding="8,4"
                                        CornerRadius="4">
                                    <TextBlock Text="{x:Bind StatusText, Mode=OneWay}"
                                               Foreground="White"
                                               FontSize="12"/>
                                </Border>

                                <TextBlock Grid.Row="1" Grid.Column="0"
                                           Text="位置: {x:Bind Location, Mode=OneWay}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                                <TextBlock Grid.Row="2" Grid.Column="0"
                                           Text="地址: {x:Bind Host, Mode=OneWay}:{x:Bind Port, Mode=OneWay}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                                <TextBlock Grid.Row="3" Grid.Column="0"
                                           Text="带宽: {x:Bind Bandwidth, Mode=OneWay} Mbps"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                                <TextBlock Grid.Row="4" Grid.Column="0"
                                           Text="在线: {x:Bind Online, Mode=OneWay}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- 加载指示器 -->
            <ProgressRing x:Name="LoadingRing"
                          IsActive="False"
                          Width="48"
                          Height="48"
                          HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
