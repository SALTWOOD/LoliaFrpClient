<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LoliaFrpClient.Pages.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer x:Name="MainScrollViewer"
                  VerticalScrollBarVisibility="Auto"
                  Padding="20,20,20,40">
        <StackPanel Spacing="16" MaxWidth="800" HorizontalAlignment="Left">

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="20" BorderThickness="1"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <StackPanel Spacing="12">
                    <TextBlock Text="账户设置" FontSize="20" FontWeight="SemiBold" />
                    <StackPanel x:Name="LoginStatusCard" Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE77B;" FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock x:Name="LoginStatusText" Text="未登录" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button x:Name="LoginButton" Content="登录" Click="LoginButton_Click" MinWidth="100" Style="{StaticResource AccentButtonStyle}"/>
                        <Button x:Name="LogoutButton" Content="退出登录" Click="LogoutButton_Click" MinWidth="100" Visibility="Collapsed"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="20" BorderThickness="1"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <StackPanel Spacing="16">
                    <TextBlock Text="客户端更新" FontSize="20" FontWeight="SemiBold" />
                    <Grid ColumnSpacing="24" RowSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="当前版本:" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" x:Name="ClientCurrentVersionText" Text="v0.0.0" FontWeight="SemiBold" VerticalAlignment="Center" />

                        <TextBlock Grid.Row="1" Text="最新版本:" VerticalAlignment="Center" />
                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Spacing="12">
                            <TextBlock x:Name="ClientLatestVersionText" Text="未检查" VerticalAlignment="Center" FontWeight="SemiBold" />
                            <Button x:Name="CheckClientUpdateButton" Content="检查更新" Click="CheckClientUpdateButton_Click" Padding="12,4" />
                        </StackPanel>
                    </Grid>

                    <ToggleSwitch x:Name="AutoCheckUpdateToggle" Header="启动时自动检查更新" Toggled="AutoCheckUpdateToggle_Toggled" />
                    
                    <InfoBar x:Name="ClientUpdateInfoBar" IsOpen="False" IsClosable="False" Title="发现新版本">
                        <InfoBar.ActionButton>
                            <Button x:Name="ClientUpdateButton" Content="前往下载" Click="ClientUpdateButton_Click" />
                        </InfoBar.ActionButton>
                    </InfoBar>
                </StackPanel>
            </Border>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="20" BorderThickness="1"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <StackPanel Spacing="12">
                    <TextBlock Text="下载设置" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock Text="GitHub 镜像源预设：" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                    <RadioButtons x:Name="GitHubMirrorRadioButtons" SelectionChanged="GitHubMirrorRadioButtons_SelectionChanged">
                        <RadioButton Content="直接访问 (github.com)" 
                                     Tag="https://github.com/{owner}/{repo}/releases/download/{tag}/{asset}" />
                        <RadioButton Content="镜像源 (cdn.akaere.online)" 
                                     Tag="https://cdn.akaere.online/github.com/{owner}/{repo}/releases/download/{tag}/{asset}" />
                    </RadioButtons>
                    
                    <TextBox Header="自定义下载模板" 
                             x:Name="CustomUrlTemplateBox"
                             TextWrapping="NoWrap"
                             LostFocus="CustomUrlTemplateBox_LostFocus"
                             PlaceholderText="https://.../{owner}/{repo}/releases/download/{tag}/{asset}"/>
                </StackPanel>
            </Border>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="20" BorderThickness="1"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <StackPanel Spacing="16">
                    <TextBlock Text="核心版本管理 (frpc)" FontSize="20" FontWeight="SemiBold" />
                    <Grid ColumnSpacing="24" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" /> 
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" /> 
                            <RowDefinition Height="*" /> 
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
    
                        <TextBlock Text="当前版本:" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="CurrentVersionText" Text="未安装" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="最新版本:" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Spacing="8">
                            <TextBlock x:Name="LatestVersionText" Text="检查中..." VerticalAlignment="Center" FontWeight="SemiBold" />
                            <Button x:Name="RefreshVersionButton" Content="刷新" Click="RefreshVersionButton_Click" Padding="8,2" FontSize="12"/>
                        </StackPanel>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="状态:" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Spacing="12">
                            <TextBlock x:Name="InstallStatusText" Text="未安装" VerticalAlignment="Center" />
                            <TextBlock Text="|" Foreground="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                            <TextBlock x:Name="ProcessStatusText" Text="未运行" VerticalAlignment="Center" />
                        </StackPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button x:Name="InstallButton" Content="安装" Click="InstallButton_Click" MinWidth="100" />
                        <Button x:Name="UpdateButton" Content="更新" Click="UpdateButton_Click" MinWidth="100" IsEnabled="False" />
                        <Button x:Name="UninstallButton" Content="卸载" Click="UninstallButton_Click" MinWidth="100" IsEnabled="False" />
                    </StackPanel>

                    <StackPanel Spacing="4">
                        <ProgressBar x:Name="DownloadProgressBar" Visibility="Collapsed" Height="4" />
                        <TextBlock x:Name="ProgressText" Visibility="Collapsed" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
